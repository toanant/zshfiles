typeset -ga tunnel
tunnel=(git ssh irssi)

local command=${1%% *}

# automatically use `proxychains` for commands in `$tunnel`
if (( ${+tunnel[(r)$command]} )); then
  export LD_PRELOAD=libproxychains.so.3
fi
